import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide Next.js "N" dev indicator (bottom-left) — dev only, never shown in prod
  devIndicators: false,
};

export default nextConfig;
