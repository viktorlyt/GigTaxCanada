# PostgreSQL (Docker compose uses host port 5434)
DATABASE_URL="postgresql://gigtax:gigtax@127.0.0.1:5434/gigtax?schema=public&sslmode=disable"

# JWT
JWT_SECRET="change-me-use-long-random-string-in-production"
JWT_EXPIRES_IN="7d"

# API
PORT=4000
WEB_ORIGIN="http://localhost:3000"